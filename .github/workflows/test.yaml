name: Test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/test.yaml
      - flake.nix
      - flake.lock
      - home-modules/**
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - .github/workflows/test.yaml
      - flake.nix
      - flake.lock
      - home-modules/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  user-repo:
    name: Test config-repository-based setup
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - ubuntu-24.04
          - ubuntu-26.04
    uses: ut-issl/issl-ubuntu-environment-setup/.github/workflows/test-environment.yaml@4526ab23468473b0984468fc7eabaa3227e5578f # v0.5.1
    with:
      setup-mode: repository-based
      os: ${{ matrix.os }}
      common-repository: ut-issl/issl-ubuntu-environment-setup
      common-ref: 4526ab23468473b0984468fc7eabaa3227e5578f # v0.5.1
      user-repository: ${{ github.repository }}
      user-ref: ${{ github.sha }}
      override-issl-input: false
      timeout-minutes: 10
