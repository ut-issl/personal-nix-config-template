name: Test

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/test.yaml
      - flake.nix
      - flake.lock
      - home-modules/**
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - .github/workflows/test.yaml
      - flake.nix
      - flake.lock
      - home-modules/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  user-repo:
    name: Test config-repository-based setup
    uses: ut-issl/issl-ubuntu-environment-setup/.github/workflows/test-environment.yaml@5f2201e084f0d79565ea17c165274629f1d6b9bc # v0.2.5
    with:
      setup-mode: repository-based
      common-repository: ut-issl/issl-ubuntu-environment-setup
      common-ref: 5f2201e084f0d79565ea17c165274629f1d6b9bc # v0.2.5
      user-repository: ${{ github.repository }}
      user-ref: ${{ github.sha }}
      override-issl-input: false
